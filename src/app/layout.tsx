import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'China Leasing Intelligence | Exclusive Market Intelligence',
  description: 'Exclusive intelligence on China\'s $2.5 trillion leasing industry. Get insider insights on finance leasing, shadow banking, and alternative lending.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
